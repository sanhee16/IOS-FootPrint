//
//  SaveDefaultTripIconUseCase.swift
//  Footprint
//
//  Created by sandy on 11/14/24.
//

class SaveDefaultTripIconUseCase {
    var tripIconRepository: TripIconRepository
    
    init(tripIconRepository: TripIconRepository) {
        self.tripIconRepository = tripIconRepository
    }
    
    func execute() {
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.bread)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.accountingBillStack)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.noTalking)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.amusementPark)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.athleticsRunning)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.awardRibbonStar)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.bandageLeg)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.bicycle)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.bookmark)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.bus)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.camera)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.capsule)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.car)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.champagne)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.circle)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.cloud)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.cloudSnow)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.clover)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.cocktail)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.coffee)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.confetti)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.diamond)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.disapointed)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.dislike)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.earthStar)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.facebook)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.fire)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.fitnessBicycle)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.fitnessShaker)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.golfHole)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.google)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.heart)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.house)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.iceCream)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.instagram)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.like)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.lol)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.mad)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.medical)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.metro)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.movie)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.music)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.natural)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.nightMoon)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.officeOutdoors)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.paperEdit)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.plane)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.prank)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.rankingStarsRibbon)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.restaurantDishes)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.running)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.shoppingCart)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.smile)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.soccerField)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.spaceAstronaut)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.square)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.stampsImage)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.star)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.sun)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.tags)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.triangle)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.tripRoad)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.umbrella)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.unhappy)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.walking)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.yoga)
        let _ = self.tripIconRepository.saveTripIcon(icon: TripIcon.youtube)
    }
}
